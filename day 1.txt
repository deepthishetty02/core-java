what is java
java is a high level ,object oriented interpreted language,java uses classes and objects methadologies
in the program.it as JDK,JRE,JVM where it gives run time environment,verifies code,executes code and as 
several libraries to use .



explain how java executes
after writing the code and saved need to click on the cmd and give the comments like javac filename.java this comment is known as java compiler
 used to compile the code where it turns from object code to byte code, after that next comment java filename is known as jvm where it convert byte code 
 to machine code after entering these comments java program will be executed and provide output in cmd.
 
 
 
 what is datatype
 datatype tells the variable what type of data or value variable is holding.it also holds the size of the value,
 and in java it as two types primitive  and non primitive datatype .
 
primitive data type -these are the pre defined data types 
           Types and default values 
          1.int-0
		  2.float-0.0f
		  3.double-0.0
		  4.char-\u0000
		  5.long-0
		  6.short-0
		  7.boolean-false
non primitive datatype- these are the user defined datatypes
         Types and default values
		 1.string-null
		 2.array-null
		 3.classes-null
		 4.interface-null
		 
class java{
public static void main(String[]args){

      int a=10;
	  float b= 5.5f;
	  double c=5.67;
	  char  d='a'
	  long r=2222222L;
	  short-2;
	  boolean isOk=true;
	  String name="deep";
	  String[] name={"deep","ram","gagana"};
	  
	  }}
	  
	  
	  
variables and types
variable is an container used to store the value or data ,it holds different types of values.
 syntax - datatype variable name= values;
 
 types
 
 1.local variable-it is used to declare inside the method and can access inside the method.
 2.static variable-it is used to declare inside the class outside the method using static keyword.
 it can print using classname.variablename
 3.instance variable -it is used declare inside the class outside the method it can initialize using 
 object reference and constructor
 4.parameterised variable-it is used to declare inside the method parenthesis.
 
 class Ac{
   static int a=10;  //static
   string name="deep";  //instance
public static void main(String[]args){

   int a= 50;  //local
 static void getprice(string a){  //parameterised
 
 }
  
 }



what is array
array is a collection of homogeneous elements ,and it store multiple values
it can print each element using index value and all values will be print using for each
syntax= datatype[] arrayname={ values.......};



method and types
method is a block of code used to perform specific task.

types
without returntype without parameter- method as no returntype and as no parameter inside parenthesis
without returntype without parameter-method as no returntype and as parameters inside the parenthesis
with returntype with parameter- method as returntype and parameter inside parenthesis
with returntype no parameter - method as returntypeand no parameterin parenthesis


constructor
constructor is a special type of method used to initialize the objects.constructor name should be same as classname.
Syntax- access specifiers classname(){}

	  